# Import Dracula theme
import:
    - ~/.config/alacritty/dracula.yml

window:
    padding:
        x: 8
        y: 8

    # Spread padding around content evenly
    dynamic_padding: false

    # Window opacity
    opacity: 0.8
